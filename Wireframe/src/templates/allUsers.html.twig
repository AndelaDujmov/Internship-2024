<!DOCTYPE html>
<html>
    <head> 
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Response</title>  
    </head>
    <body>
        {% if users is iterable %}
            <p>Content: {{ users|json_encode(constant('JSON_PRETTY_PRINT')) }}</p>
        {% else %}
            <p>Content: {{ users }}</p>
        {% endif %}
    </body>
</html>