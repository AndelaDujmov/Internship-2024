{% extends 'base.html.twig' %}

{% block title %}Create Form{% endblock %}

{% block body %}

    <h1>{{ teams.getTeam().getName() ?? null }}</h1>

    {{ form_errors(form) }}

    {{ form_start(form) }}
        {{ form_row(form.teamLead) }}
        {{ form_row(form.projectLeader) }}

        <button type="submit" class="btn">Create</button>
    {{ form_end(form) }}
{% endblock %}